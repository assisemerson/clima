<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/city_card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/cards_layout_margin"
    android:clickable="true"
    card_view:cardElevation="2dp"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/city_image"
            android:layout_width="@dimen/city_image_size"
            android:layout_height="@dimen/city_image_size"
            android:src="@drawable/city_image"
            android:tint="@color/colorAccent"
            android:layout_margin="@dimen/cards_layout_margin"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:layout_margin="@dimen/cards_layout_margin">

            <TextView
                android:id="@+id/city_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:textSize="@dimen/city_name_size"
                android:layout_margin="@dimen/city_margin_size"
                android:layout_gravity="center_vertical"/>

            <TextView
                android:id="@+id/city_uf"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:textSize="@dimen/city_uf_size"
                android:textColor="@color/colorPrimary"
                android:layout_gravity="center_vertical"/>
        </LinearLayout>
    </LinearLayout>

</android.support.v7.widget.CardView>